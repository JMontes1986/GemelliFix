rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite la lectura y escritura a cualquier usuario autenticado
    // Esto es para depurar el problema de permisos.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
